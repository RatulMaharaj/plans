---
"plans": minor
---

The chat is now one conversation per repository rather than one per file.
Clicking another plan no longer resets it; which plan you are looking at rides
the next turn as a line of context instead of partitioning the transcript.
Existing per-file transcripts are left where they are — merging several into
one would produce a conversation nobody had.
