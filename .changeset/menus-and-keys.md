---
"plans": patch
---

The tree's right-click menu stays on screen: near the bottom or right edge it
flips and clamps instead of running off the window.

⌘⌫ deletes the open file, after asking — except while the caret is in the page,
where it still deletes to the start of the line. F2 renames it.

The finished-plans setting says what it does: "Show finished plans", shown or
hidden, rather than a switch whose "off" read as though it turned the plans off.
