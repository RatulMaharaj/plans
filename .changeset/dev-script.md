---
"plans": patch
---

`pnpm dev` is the app; `pnpm web` is the web half alone, for a browser. A
committed `.env` gives local builds the workspace server's address, so the
app run from source has workspaces too.
