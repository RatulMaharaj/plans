# Read by Vite for every local build and `pnpm dev`. Nothing here is secret:
# the workspace server's address is public, and the release workflow sets the
# same variable from the repository's WORKSPACE_URL.
VITE_WORKSPACE_URL=https://plans.looped.sh
